import 'package:flutter/material.dart';

class ProduitPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Produit'),
      ),
      body: Center(
        child: Text('Contenu du Produit'),
      ),
    );
  }
}
