import 'package:flutter/material.dart';

class HomeInvestisseur extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ivestisseur')),
      body: Center(child: Text('Bienvenue !')),
    );
  }
}
