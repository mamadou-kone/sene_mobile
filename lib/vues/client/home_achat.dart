import 'package:flutter/material.dart';

class HomeAchat extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Acheteur')),
      body: Center(child: Text('Bienvenue !')),
    );
  }
}
